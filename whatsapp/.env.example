# URL of the running DREAM-Chat Flask server
FLASK_BASE_URL=http://localhost:8000

# Credentials for the bot's Flask account
# Add this user to the USERS dict in app.py, e.g.:
#   USERS = { ..., "whatsapp_bot": "your-secure-password" }
FLASK_USERNAME=whatsapp_bot
FLASK_PASSWORD=your-secure-password

# Log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info

# Optional SOCKS5/HTTP proxy for routing WhatsApp WebSocket traffic
# Required if running behind a firewall (e.g. mainland China)
# WA_PROXY_URL=socks5://127.0.0.1:7897

# ── Personal-number mode (default) ───────────────────────────────────────────
#
# ASSISTANT_HAS_OWN_NUMBER
#   Set to "true" only when the linked WhatsApp account is a dedicated bot
#   number that no human uses for personal messaging.
#   Default: false (personal-number mode)
# ASSISTANT_HAS_OWN_NUMBER=false

# ASSISTANT_NAME
#   Prefix added to every bot reply in personal-number mode so you can
#   distinguish bot messages from your own messages in the same thread.
#   Default: DREAM
# ASSISTANT_NAME=DREAM

# ALLOWLIST_JIDS
#   Comma-separated list of phone numbers (with country code) that the bot
#   will respond to. Strongly recommended on a personal number to prevent
#   the bot from replying to every contact.
#   Self-chat (your own number) is always allowed regardless of this list.
#   Leave empty to allow all senders (only safe on a dedicated bot number).
#   Example: +8613812345678,+12025551234
# ALLOWLIST_JIDS=
